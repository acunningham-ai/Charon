{
  "$comment": "Vault-level MCP server registry — checked into the repo. The vault-ops server is wired via .claude/settings.json; this file adds vault-readonly and vault-graph as separately-enabled-by-default servers. vault-graph tools fail gracefully if the optional networkx dep isn't installed (see requirements-graph.txt).",
  "mcpServers": {
    "vault-readonly": {
      "type": "stdio",
      "command": "python",
      "args": [
        "${CLAUDE_PROJECT_DIR}/scripts/mcp/vault-readonly-server.py"
      ]
    },
    "vault-graph": {
      "type": "stdio",
      "command": "python",
      "args": [
        "${CLAUDE_PROJECT_DIR}/scripts/mcp/vault-graph-server.py"
      ]
    }
  }
}
